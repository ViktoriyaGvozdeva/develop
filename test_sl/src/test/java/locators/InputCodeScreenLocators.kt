package locators

import constructor_classes.LocatorsConstructor

class InputCodeScreenLocators {
    val pinCodeEditText = LocatorsConstructor(
        androidId = "ru.sportmaster.app.handh.dev:id/pinCodeEditText"
    )
}